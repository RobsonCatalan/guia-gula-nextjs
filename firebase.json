{
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run predeploy:clean",
      "npm --prefix \"$RESOURCE_DIR\" run predeploy:copy"
    ]
  },
  "hosting": {
    "public": "public/",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      { "source": "**", "function": "ssr" }
    ]
  }
}
